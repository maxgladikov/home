spring: 
    profiles:
      active: dev
    cloud:
        config:
            name: telegram-bot 
#            uri: http://home.config:8888
            uri: http://localhost:8888
            label: main
        refresh:
          enabled: false
logging:
  level:
    root: INFO
